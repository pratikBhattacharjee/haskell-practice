import Data.List
import System.IO

--Int allows from -2^63 to 2^63 -1
maxInt = maxBound :: Int
minInt = minBound :: Int

--Integer is another one and it can be any size, but it is slower than Int
--Integer is not limited to 64 bits, but it is slower than Int


--Bool is a boolean value, it can be True or False
--Char is a single character, it can be any character in the Unicode set


{-Practice Example to try data types in Haskell-}
--Practice file Zoo.hs